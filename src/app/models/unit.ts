/* Unit is the parent of ones, tens, specialcase and hundreds. 
 * The four classes has the same properties and the same concept .
 */

export class Unit {

    // Array as map => array[index] = number of letters 
    public _mapDigits: any[];
    public numberOfLetters: number;

}
